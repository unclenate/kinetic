# Project Change Log

This log records material changes to project scope, plan, timeline, or technical
direction. It is not a git commit log — it captures *decisions and their rationale*.

---

## Log

| Date | Type | Change | Reason | Owner | ADR |
| ---- | ---- | ------ | ------ | ----- | --- |
| 2026-06-03 | Technical | Phase A (pluggable LLM providers) + Phase B (privacy-aware routing) landed. Phase A: `src/providers/registry.mjs` (`runProvider` validate+1-retry, `residencyOf`, `isAvailable`), local `ollama` + `openai` providers, refit claude/gemini/mock to `process(input, opts)`, server/regression routed through the registry (live-verified against local Ollama). Phase B: `src/providers/router.mjs` resolves each capture (override → source-pin → hint-rule → default) on pre-LLM signals; `domain-hint` gains an `unknown` (no-signal) value so uncertain→local; server enforces fail-closed (sensitive + local provider unavailable → 503) and a cloud-acknowledgment gate (sensitive→cloud override without ack → 400). Routing is opt-in via `KINETIC_PROVIDER=auto`; default forces mock (zero-setup demo preserved). At-rest encryption of sensitive rows is Phase C. | Make the LLM layer pluggable and deliver the privacy-by-design routing core (sensitive stays on-device). | @unclenate | ADR-0006 (pending, Phase C) |
| 2026-05-16 | Scope | Initial scope captured: hackathon v0 = single-user demo, image+text capture, public share link | Distillation of `docs/discovery/inbox/` concept doc | @unclenate | — |
| 2026-05-16 | Technical | Stack selected: Node/TS + Supabase + Gemini (Claude fallback); composition starts as `new-product-discovery`, will migrate after hackathon | Hackathon timebox; need rapid iteration without tripping required-artifact validations | @unclenate | ADR-0001 |
| 2026-05-16 | Scope | Voice memo capture moved from Must to Should tier for v0 | Demo can land without it; image+text covers the magic moment | @unclenate | — |
| 2026-05-16 | Scope | Auth deferred for v0; single hardcoded demo user | Demo-time risk; auth flow is the wrong place to spend hackathon hours | @unclenate | — |
| 2026-05-16 | Technical | M1 landed: JSON schema + prompt template + zero-dependency Node ESM regression harness + Gemini and Claude providers. Mock provider passes 10/10 schema-valid. Real-provider runs deferred until API keys are configured. | Lock the highest-risk dependency before building UI. Zero-dep `.mjs` avoids `npm install` time-cost during the 72-min build window; TypeScript migration captured as a follow-up. | @unclenate | ADR-0001 |
| 2026-05-16 | Technical | Real-LLM regression measured: Claude 10/10, Gemini 8/10 (failures = 429/503 rate-limit at venue shared-IP NAT, not schema violations). Demo at venue will default to Claude. | M1 quality gate measured against real LLMs, not just the mock | @unclenate | — |
| 2026-05-16 | Scope | Supabase persistence deferred from M2 to P3; v0 uses in-memory `Map` instead. RLS criterion for M3 likewise deferred — replaced for v0 with an `isPublic` flag on each card. | Hackathon time budget; in-memory is sufficient for the demo and removes a vendor wiring step that wasn't on the critical path. | @unclenate | — |
| 2026-05-16 | Technical | M2 + M3 landed: zero-dependency Node HTTP server (`web/server.mjs`), single-page capture UI, public share-link page. End-to-end loop verified in Chrome with screenshots committed under `docs/screenshots/`. | Demoable artifact for the submission. | @unclenate | — |
| 2026-05-16 | Scope | Added signal-harvester contract and two implementations: GitHub (public events, no auth) and Calendar (pasted-text v0 seam). Three new endpoints: `POST /api/harvest/github`, `POST /api/harvest/calendar`. UI gains a tabbed "Harvest from a signal source" panel that displays generated Proof cards inline. | Demonstrates that Kinetic is source-agnostic — every capture goes through the same M1 LLM contract. Google / Outlook OAuth harvesters can slot into the same `harvest()` interface later. | @unclenate | — |
| 2026-05-16 | Scope | Added third harvester: real Google Calendar API v3 (`src/harvesters/gcal.mjs`). Auth via OAuth 2.0 access token (`calendar.readonly` scope). Token is sourced from OAuth Playground for the demo (~1h lifetime) and passed per-request in the POST body, or via `GOOGLE_ACCESS_TOKEN` env var. UI gets a new "Google Cal" tab with token input. | Promotes the calendar story from "pasted-text seam" to a real, live API integration without paying the full OAuth-registration cost. Same `harvest()` contract — proves the architecture handles real OAuth sources. | @unclenate | — |
| 2026-05-16 | Technical | Live gcal harvest verified end-to-end via the UI against the operator's real Google Calendar. 6 events pulled, 3 Proof cards generated by Gemini (including a meta card for this hackathon itself). Screenshot committed: `docs/screenshots/07-harvest-gcal-live.png`. | M1 contract proven against a real OAuth-protected API, not just public-no-auth (GitHub) or text seams (pasted). | @unclenate | — |
| 2026-06-02 | Scope | Ambiguous-domain default resolved: a capture with no life-domain signal classifies as `business` (the public-eligible side of the privacy gate). Operator decision. Safe under the current explicit-share model (cards default `is_public:false`); flagged to revisit if auto-publish/bulk-share is added. | ADR-0002/0003 flagged this to settle before M10; M10 lands the privacy-gate UI. | @unclenate | ADR-0003 |
| 2026-06-02 | Technical | M10 (code) landed: multi-domain feed + privacy gate. `store.listCards()` on both backends; `GET /api/cards` (feed) + `GET /api/connections` (OAuth status); UI feed with `All/Business/Personal/Family/Financial/Parenting` filter tabs (client-side, instant), connection-status header chips, and an un-skippable share-confirmation modal that names the domain for non-business cards (`requestShare()`); `tools/privacy-audit.mjs` (pure `auditPublicCards()` + CLI) asserting every public card is `domain=business`. Backend 4/4 offline tests; endpoints verified live; UI verified statically (parses, selectors resolve) — live DOM render pending a clean browser session. Live OAuth-connected state pending M7 credentials. | The visible side of the cross-domain wedge: domains are filterable, and non-business captures cannot reach the public surface without an explicit, domain-named confirmation. | @unclenate | ADR-0002, ADR-0003 |
| 2026-06-02 | Technical | M7 (code) scaffolded offline: OAuth authorization-code + PKCE coordinator (`src/oauth/`: crypto AES-256-GCM, pkce, providers, index, token-store), Supabase PostgREST persistence (`src/db/supabase.mjs` + pluggable `store.mjs` with memory↔supabase backends), `db/schema.sql`, and server wiring (`/oauth/:provider/start|callback`, store-backed persistence). Built test-first: 18 offline tests (`tests/oauth.test.mjs` + `tests/store.test.mjs`) cover token encrypt/decrypt + tamper, PKCE, auth-URL building, token exchange/refresh, memory store, PostgREST request-building, and encrypted-token save + refresh-on-load — all with stubbed `fetch`/no creds. Demo still runs on memory (verified live); OAuth routes degrade to "not configured" without credentials. `.env.example` extended. **Live verification (real Google/Microsoft consent, ciphertext-in-DB, refresh against a real expiring token, restart persistence) is pending operator credential setup.** | Do real OAuth properly for the 5-day track (ADR-0004); the playground-token shortcut was right for an hour, wrong for a week. Scaffolded ahead of credentials to keep momentum. | @unclenate | ADR-0004 |
| 2026-06-02 | Technical | M9 (code) landed: five new harvesters on the existing `harvest()` contract — `mscal` (Graph calendarView), `gdrive` (Drive Activity query), `onedrive` (Graph /me/drive/recent), `gmail_sent` (Gmail list + metadata-only get), `outlook_sent` (Graph sentitems). Each emits the canonical capture shape plus a `provider_domain_hint` (shared `domain-hint.mjs` heuristics: recipient email domains, file path, subject keywords). Server harvest-route regex widened to `[a-z0-9_]+` for underscored names. Built test-first: `tests/harvesters.test.mjs` exercises the full harvest() path with a stubbed `fetch` (12/12 green, zero credentials); `npm test` now runs selftest + regression + harvesters. **Live verification against real accounts remains gated on M7 OAuth** — these are offline-complete, not yet account-verified. | Keep the v0.5 build moving while OAuth/Supabase credentials are provisioned; the harvesters light up unchanged once M7 lands. | @unclenate | ADR-0002, ADR-0004 |
| 2026-06-02 | Technical | M8 landed: LLM contract migrated to the two-dimensional shape (`category`→`activity_type` rename + new required `domain` enum). Validator self-test, regression runner (now gating ≥90% schema-valid AND ≥95% domain-correct), mock provider (`classifyDomain()`), prompt, UI (domain + activity pills), and tests/README updated in lockstep. Regression set grown 10→20 fixtures (≥2/domain). Mock: 20/20 schema-valid, 20/20 domain-correct. Built test-first against the zero-dep harness. Gemini/Claude code unchanged (schema+prompt inlined at runtime); real-provider domain accuracy still pending API keys (M11). | Lock the v0.5 contract before harvesters land (M9). Self-policing rename via `additionalProperties:false`. | @unclenate | ADR-0003 |
| 2026-05-16 | Scope | v0.5 5-day-track scope expansion: project gains a second hackathon submission target (2026-05-21) on top of the already-shipped v0. v0.5 introduces real OAuth (Google + Microsoft), Supabase persistence with encrypted refresh tokens, two-dimensional LLM classification (`domain` × `activity_type`), five new harvesters (Microsoft Cal, Google Drive Activity, OneDrive, Gmail-sent, Outlook-sent), a multi-domain feed with privacy gate, and a regression refresh. Discovery work (M6) lands as ADR-0002, ADR-0003, ADR-0004 plus refreshed problem-statement, personas, requirements, mvp-scope, release-intent, scope-plan, and milestones (now M0–M12). v0 history is preserved unchanged. | Five days is enough to do real OAuth properly and seat a privacy-gated cross-domain capture story. The playground-token shortcut was right for one hour, wrong for one week. | @unclenate | ADR-0002, ADR-0003, ADR-0004 |

---

## What Belongs Here

Add an entry when:

- A requirement is added, removed, or significantly changed
- A milestone is moved or dropped
- An architectural decision changes direction
- A feature is explicitly deferred to a future release
- A third-party dependency changes (vendor, API, integration)

Do NOT add entries for routine code changes, minor doc fixes, or bug fixes that
don't change scope or direction.

---

## Reference

| Resource | Path |
| -------- | ---- |
| Requirements | [`docs/product/requirements.md`](../product/requirements.md) |
| MVP scope | [`docs/discovery/mvp-scope.md`](../discovery/mvp-scope.md) |
| ADR directory | [`docs/adr/`](../adr/) |
| Milestones | [`docs/project/milestones.md`](./milestones.md) |
