# Copyright 2026 Nate DiNiro
# SPDX-License-Identifier: MIT OR Apache-2.0
# Kinetic composition. Advanced from the discovery composition to the
# node-web-saas-postgres composition now that the stack is chosen and built
# (Node web server + Supabase Postgres + OAuth). See ADR-0005.
schemaVersion: 1
project:
  id: kinetic
  name: Kinetic (AutoPortfolio)
  maturity: alpha
  criticality: medium
modules:
  core:
    - kernel/base
  stacks:
    - node-typescript
  architectures:
    - web-app
  data:
    - relational-postgres
  delivery:
    - production-saas
  management:
    - product-lite
    - project-standard
    - testing-standard
    - knowledge-capture
  agents:
    - base
    - claude-code
overrides:
  requiredArtifacts: []
  disabledValidations: []
